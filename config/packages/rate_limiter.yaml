framework:
    rate_limiter:
        login:
            policy: 'sliding_window'
            limit: 5
            interval: '1 minute'
        registration:
            policy: 'sliding_window'
            limit: 3
            interval: '1 minute'

when@test:
    framework:
        rate_limiter:
            login:
                policy: 'sliding_window'
                limit: 1000
                interval: '1 minute'
            registration:
                policy: 'sliding_window'
                limit: 1000
                interval: '1 minute'
