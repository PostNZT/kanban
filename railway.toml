[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 300
startCommand = "bash /usr/local/bin/start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
